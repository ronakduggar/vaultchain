{
  "name": "vaultchain-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for VaultChain decentralized password manager",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  }
}
